{
  "fetch_id": {
    "name": "fetch_id",
    "author_id": 0,
    "code": "SELECT * FROM confessions WHERE id = :CNUM",
    "query": true,
    "parameters": [
      "CNUM"
    ]
  },
  "get_other_messages": {
    "name": "get_other_messages",
    "author_id": 0,
    "code": "SELECT x.id FROM confessions AS x JOIN (SELECT * FROM confessions WHERE id = :CNUM LIMIT 1) AS l ON x.author_id = l.author_id;",
    "query": true,
    "parameters": [
      "CNUM"
    ]
  }
}